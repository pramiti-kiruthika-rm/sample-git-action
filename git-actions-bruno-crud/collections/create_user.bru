request {
  name: "Create User"
  method: POST
  url: "{{baseUrl}}/users"
  headers: {
    "Content-Type": "application/json"
  }
  body: {
    "name": "John Doe",
    "email": "john@example.com"
  }
}